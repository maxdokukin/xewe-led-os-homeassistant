{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "Add XeWe LED",
        "description": "Enter the IP address shown by the device on your network.",
        "data": {
          "host": "Device IP address"
        }
      },
      "pair": {
        "title": "Pair XeWe LED",
        "description": "The device at {host} is in discovery mode. Submit to send the MQTT broker credentials to it automatically. Override the broker address only if the device cannot reach it.",
        "data": {
          "broker_override": "MQTT broker address (optional)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the device. Make sure it is still in discovery mode (press 'y' on the device to restart the 5-minute window), then try again.",
      "no_broker": "Could not determine a broker address reachable by the device. Enter one manually."
    },
    "abort": {
      "already_configured": "This device is already configured.",
      "no_mac": "The device did not advertise a MAC address and cannot be identified.",
      "mqtt_not_configured": "The MQTT integration is not set up. Install the Mosquitto broker add-on and add the MQTT integration, then try again."
    }
  },
  "issues": {
    "mqtt_not_configured": {
      "title": "XeWe LED requires MQTT",
      "description": "XeWe LED needs the MQTT integration. Install the Mosquitto broker add-on and add the MQTT integration, then reload XeWe LED."
    }
  }
}
